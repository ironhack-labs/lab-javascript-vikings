// Soldier
function Soldier (healthArg, strengthArg) {
  function attack () {
    return Soldier.strengthArg
  }
  function receiveDamage (damage) {
    soldier.healthArg -= damage
  }
}

// Viking
function Viking (name)(
  Soldier.call(this, name);

  battleCry: function(){
    return "Odin owns you all"
  }
  receiveDamage.call(this,the damage);
  if (Viking.healthArg >0){
    return (Viking.name + "is alive")
  }else {
    return (Viking.name+"is dead" )
  }

  Viking.prototype=Object.create(Soldier.prototype);


) {}

// Saxon
function Saxon () {}

// War
function War () {}
